/**
 * Kept for backwards compatibility — the canonical implementation now lives in
 * `src/lib/format.js` alongside the other formatting helpers.
 */
export { formatINR } from '../lib/format';
export { formatINR as default } from '../lib/format';
